<div class="container mt-5">
    <h1>Carrito ID: {{cid}}</h1>
    <ul class="list-group">
        {{#each products}}
            <li class="list-group-item">
                <span>{{this.quantity}}</span>
            </li>
        {{/each}}
    </ul>
</div>